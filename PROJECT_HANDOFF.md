# 剑盾纪事：唯一规则与交接文档

本文件是本项目唯一的产品规则、实现边界、当前状态和后续交接依据。后续会话先阅读本文件，再检查代码；不要依赖已删除的旧规则文件、旧视觉稿或未提交的临时产物。

## 1. 产品边界

- 产品是 Windows 10 桌面组件，技术栈固定为 Tauri 2 + React + TypeScript。
- 不建立 WPF、Electron、浏览器产品、跨平台抽象、云端、账号、AI 或协作路线。
- 数据为本地优先，SQLite 是唯一持久化数据源；React 组件不能另建第二份领域状态。
- 界面始终只有两个固定分组，分别由原始剑、盾图标识别；不恢复旧文字分组名。
- 看板模式固定为 `480x600`，另有单击进入的图标模式；不恢复已删除的展开、详情、置顶、图标拖拽路径。

## 2. Windows 行为门禁

- 窗口不置顶，位于普通窗口之后；不出现在任务栏和 Alt+Tab。
- 启动、显示、缩放、拖拽、模式切换默认不抢焦点；只有明确的文字编辑临时获取焦点，编辑结束后回到被动状态。
- 原生窗口控制只放在 `src/app/window/`；看板与领域状态边界不得混入 React 组件之外的隐式副本。
- Windows 窗口位置、拖拽、缩放、透明度、焦点和视觉质量必须通过真实 Windows 交互验收；浏览器预览不能替代原生验收。

## 3. 当前交互基线

- 两个分组内都可以直接添加任务，任务支持同组排序、跨组拖拽和标题双击编辑。
- 任务卡普通展示态统一为 `65px` 高度；标题最多显示两整行。
- 超出两行的标题在卡片内部滚动，隐藏原生滚动条，滚轮按完整行移动，不显示半行截断文字。
- 长标题初始显示向下箭头，滚到中间显示上下箭头，滚到底显示向上箭头；短标题不显示滚动标识。
- 编辑态使用可自动增高的多行文本框；Enter 保存，Shift+Enter 换行，Escape 取消，失焦保存。
- 移除仍遵循单卡片待确认、再次确认后软移除并写入历史的既有流程。
- 当前唯一底板资源是 `src/assets/pixel-board-frame-v8.png`。不要恢复或新增 v3-v7 等旧版本资源。

## 4. 代码边界

- `src/board/`：任务模型、领域变换、SQLite 仓储及持久化边界。
- `src/features/floating-board/`：看板、分组、任务卡、拖拽、编辑和滚动展示。
- `src/features/history/`：移除历史展示。
- `src/app/`：应用壳、样式、窗口控制和模式切换。
- 视觉调整应优先修改对应组件和 `src/app/styles.css`，不得通过新增第二套数据源或测试专用分支掩盖错误。

## 5. 变更协议

每次变更开始前先明确：观察到的行为、所属层、最小文件范围和验证方式。缺陷必须沿完整流程找到最早错误阶段，在拥有该行为的层修复；不要叠加下游补偿、重复 fallback、重试或无关重构。

每次交付至少执行：

1. `npm test -- --run`
2. `npm run build`
3. 若涉及 Windows 或安装行为，执行 `npm run tauri build`，并实际安装/启动对应产物。
4. 对视觉、焦点、拖拽、窗口层级和滚动行为，记录实际观察结果；未验证的验收项必须明确写出。

提交时只纳入本次变更相关文件，不覆盖或删除用户已有的无关改动。仓库交付前保持本地 `main` 与 `origin/main` 一致。

## 6. 当前状态

- 当前正式 UI 底板为 v8，旧 v3-v7 资源已废弃，不应保留。
- 任务卡长标题展示、隐藏滚动条、整行滚动和上下箭头标识已实现。
- 长标题编辑框自动增高已实现。
- 现有领域与持久化测试通过；生产构建和 Tauri Windows 安装包已验证成功。
- 原生 Windows 的完整真人视觉/手感验收仍需在可用的真实桌面交互环境中复核，不能用浏览器截图冒充。

## 7. 交接要求

后续更新只以本文件为规则和进度真相。完成改动后更新“当前状态”中受影响的条目，并保持文件本身、源代码、安装产物和 `origin/main` 的状态一致。任何未解决问题、未验证门禁或临时假设都必须显式记录。
