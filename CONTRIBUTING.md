# 参与贡献

感谢你对剑盾纪事感兴趣。项目当前聚焦 Windows 10/11，技术栈固定为 Tauri 2、React 和 TypeScript。

## 开始之前

- 先在 Issue 中描述用户可观察的问题、复现方式和期望行为。
- 保持一个改动只解决一个问题，不引入云端、账号、协作或跨平台抽象。
- 保留两个固定的剑/盾分组，以及 `480 × 600` 看板和 `52 × 52` 单 Logo 两种模式。
- 不要让 React 组件绕过领域状态或持久化边界成为第二份数据源。

## 本地检查

提交前至少运行：

```bash
npm test
npm run build
cargo test --manifest-path src-tauri/Cargo.toml
```

原生窗口行为不能只用 DOM 自动化证明。涉及窗口层级、焦点、拖拽、透明边缘、缩放或安装器的 PR，请说明 Windows 版本、缩放比例和真实验证结果，并附截图或录屏。

## 提交与 PR

- 使用清晰、具体的提交信息。
- PR 描述应包含：问题、最早错误层、解决方式、测试结果和仍未验证的内容。
- 不要为了通过测试降低断言，也不要提交构建目录、依赖目录或设计废稿。
